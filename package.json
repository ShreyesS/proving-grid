{
  "name": "proving-grid",
  "private": true,
  "scripts": {
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "setup:frontend": "cd frontend && npm install",
    "dev": "concurrently --kill-others-on-fail -n api,ui -c blue,green \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd backend && .venv/bin/uvicorn main:app --reload --port 8000",
    "dev:ui": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
